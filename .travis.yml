matrix:
  include:
    - language: python
      env:
            -DJANGO=1.8 DB=sqlite
      install: # => 5
        - pip install -r requirements.txt

      script:
        - python manage.py test
    - language: node_js

      before_script:
        - cd frontend
        - chmod +x jest
      script:
        - npm run test